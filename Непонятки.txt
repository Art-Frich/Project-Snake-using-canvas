Вопросы по проекту:

1) var context = canvas.getContext('2d');
что в python, что здесь - canvas. 
Нужно разобраться в том, как он работает.
Как его применять и где.
// Очищаем игровое поле
  context.clearRect(0, 0, canvas.width, canvas.height);
Что мы делаем с context?

2) var count = 0;
 // Дальше будет хитрая функция, которая замедляет скорость игры с 60 кадров в секунду до 15. Для этого она пропускает три кадра из четырёх, то есть срабатывает каждый четвёртый кадр игры. Было 60 кадров в секунду, станет 15.
  requestAnimationFrame(loop);
Нужно разобраться в данной функции
// Игровой код выполнится только один раз из четырёх, в этом и суть замедления кадров, а пока переменная count меньше четырёх, код выполняться не будет.
  if (++count < 4) {
    return;
  }
  // Обнуляем переменную скорости
  count = 0;

  3) почему начальные размеры canvas не совпадают с размерами блока в css